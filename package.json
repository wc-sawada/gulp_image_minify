{
  "name": "gulp_base_repository",
  "version": "1.0.0",
  "description": "画像圧縮のみのgulpタスク",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-changed": "^3.2.0",
    "gulp-imagemin": "^4.1.0",
    "gulp-watch": "^5.0.1",
    "imagemin-pngquant": "^6.0.0"
  }
}
